[pytest]
addopts = --embedded-services idf,qemu --build-dir build_esp32c3_qemu
markers =
    host_test: off-device tests
    qemu: tests that run in QEMU
norecursedirs = build build_* managed_components
